# Collection of dockerfiles to build docker images

Docker images are also published on the docker hub at my profile [mschnepf](https://hub.docker.com/u/mschnepf). All current images are for HEP environments.
